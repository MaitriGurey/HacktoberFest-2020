#include <stdio.h>

int main()
{
	int num,pg;
	scanf("%d", &num);              			// Reading input from STDIN
    scanf("%d", &pg);
    if(num<=23&&pg>500||pg<1000)
    printf("Take Medicine");
    else
    printf("Don't take Medicine");
}
